BOT_TOKEN=123456:ABC-DEF...

# Base URL of the vdownloader worker (file server + REST API on the same port).
WORKER_URL=http://localhost:8080

# Address this bot's webhook receiver listens on.
# The worker must be configured with WEBHOOK_URL=http://<this-host>:8090/webhook
WEBHOOK_ADDR=:8090
